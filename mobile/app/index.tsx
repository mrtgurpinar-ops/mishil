/**
 * Mışıl Baby — Tekil Kod Tabanı (Single Source of Truth)
 *
 * Tüm UI/UX, ekranlar, mantık ve animasyonlar:
 *   → projects/mishil/public/app.html (Railway canlı)
 *
 * Bu bileşen yalnızca native köprüleri (IAP, haptik, mikrofon) sağlar.
 * Artık ayrı React Native sekme ekranları yoktur.
 */
import MishilUnifiedWebView from '../components/MishilUnifiedWebView';
export default MishilUnifiedWebView;
