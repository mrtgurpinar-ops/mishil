[providers]
providers = ["python"]

[phases.setup]
nixPkgs = ["libsndfile", "ffmpeg"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python app/main.py"
